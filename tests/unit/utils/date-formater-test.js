import dateFormater from 'recover/utils/date-formater';
import { module, test } from 'qunit';

module('Unit | Utility | date-formater', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dateFormater();
    assert.ok(result);
  });
});
